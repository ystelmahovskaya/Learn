<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="1"
        android:entries="@array/list_preference_entries"
        android:entryValues="@array/list_preference_entry_values"
        android:key="level_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Level" />

    <Preference android:title="Remove all statistics" android:key="remove_statistics">
    </Preference>
    <Preference android:title="Remove my profile" android:key="remove_profile">
    </Preference>

</PreferenceScreen>
